# Patients Management API

A simple FastAPI application to manage patient records using JSON storage.

## Features
- View all patients
- View patient by ID
- FastAPI + JSON backend

## Run the App
```bash
pip install -r requirements.txt
uvicorn main:app --reload
